<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /aes-project-tracker/

    # Redirect direct non-file requests to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L,QSA]
</IfModule>
